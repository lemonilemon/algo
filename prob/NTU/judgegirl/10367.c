#include <stdio.h>
#include <stdbool.h>
#define sqr(x) ((x) * (x))
int main() {
    int xa, ya, ra, xb, yb, rb;
    scanf("%d%d%d%d%d%d", &xa, &ya, &ra, &xb, &yb, &rb);

    bool ina = sqr(xa - , inb;
}
